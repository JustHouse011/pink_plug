# No application environment variables are currently required or read.
# The frontend runs with bundled mock data and local state.
# See docs/ENVIRONMENT.md before introducing backend configuration.
# Never place server credentials or private keys in EXPO_PUBLIC_* variables.
